package server;

/**
 * Interface: FileSynchronizationServer
 * Description: General interface for a file synchronization server.
 */
public interface FileSynchronizationServer {
    void listen();
}
